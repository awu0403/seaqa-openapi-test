# This GitHub Actions workflow was auto-generated by the `rdme` cli on 2023-12-21T08:24:07.680Z
# You can view our full documentation here: https://docs.readme.com/docs/rdme
name: ReadMe GitHub Action 🦉

on:
  push:
    branches:
      # This workflow will run every time you push code to the following branch: `v1.0.1`
      # Check out GitHub's docs for more info on configuring this:
      # https://docs.github.com/actions/using-workflows/events-that-trigger-workflows
      - master
  
jobs:
  rdme-docs:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo 📚
        uses: actions/checkout@v3

      # Seaqa API (docs only)
      - name: Update Seaqa API 🚀
        uses: readmeio/rdme@v8
        with:
          rdme: docs ./reference --key=${{ secrets.README_API_KEY }} --version=1.0

      # ticket operations:  user
      - name: Ticket operations 🚀
        uses: readmeio/rdme@v8
        with:
          rdme: openapi ./api/ticket_operations.yaml --key=${{ secrets.README_API_KEY }} --id=67b7fcf56714a0002932b301 --useSpecVersion
